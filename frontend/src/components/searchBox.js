import React from "react";

const SearchBox = () => {
  // You need a text input field and a submit button
  // The text input when it changes need to save its data to a local state (using setState())
  // The submit button will grab the data from the text input
  // Add your own instructions and stuff
  return (
    <div>
      <h1>Search</h1>
    </div>
  );
};

export default SearchBox;
